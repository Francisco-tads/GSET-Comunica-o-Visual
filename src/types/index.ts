export interface ServiceProps {
  id: string;
  title: string;
  description: string;
  imageUrl: string;
}

export interface PortfolioItemProps {
  id: string;
  title: string;
  category: string;
  imageUrl: string;
  whatsappNumber: string;
}